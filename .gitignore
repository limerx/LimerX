node_modules/
.next/
out/
build/
dist/
*.tsbuildinfo

.env
.env.local
.env.*.local

*.log
npm-debug.log*

.DS_Store
*.pem

# PDF sources are large and often confidential; keep them out of git by default
/data/sources/*.pdf

# Anciennement exclues, elles sont maintenant committees pour que le build de production
# (Vercel) puisse les servir depuis public/ - voir la note legale dans le README sur les
# droits du document source avant de partager publiquement.
