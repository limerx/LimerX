services:
  db:
    image: pgvector/pgvector:pg16
    restart: unless-stopped
    environment:
      POSTGRES_USER: limerx
      POSTGRES_PASSWORD: limerx
      POSTGRES_DB: limerx
    ports:
      - "5432:5432"
    volumes:
      - limerx_pgdata:/var/lib/postgresql/data

volumes:
  limerx_pgdata:
